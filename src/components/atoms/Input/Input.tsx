import styled from '@emotion/styled';

export const Input = styled.input`
  color: #ffffff;
  background-color: transparent;
  border: transparent;
  outline: transparent;
  flex-grow: 1;
  margin-left: 0.5rem;
`;
