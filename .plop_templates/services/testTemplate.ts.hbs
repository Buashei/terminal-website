import { describe, it, expect } from 'vitest';
import { {{pascalCase servicesName}} } from './{{pascalCase servicesName}}';

describe('{{pascalCase servicesName}}.ts', () => {
  it('{{camelCase servicesName}} service should be defined', () => {
    const {{camelCase servicesName}} = new {{pascalCase servicesName}}();
    expect({{camelCase servicesName}}).toBeDefined();
  });
});
