import { describe, it, expect } from 'vitest';
import { {{capitalize servicesName}} } from './{{capitalize servicesName}}';

describe('{{capitalize servicesName}}.ts', () => {
  it('{{lowerCase servicesName}} service should be defined', () => {
    const {{lowerCase servicesName}} = new {{capitalize servicesName}}();
    expect({{lowerCase servicesName}}).toBeDefined();
  });
});
